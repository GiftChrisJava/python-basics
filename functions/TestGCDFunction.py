from GCDFunction import gcd

# ask user to enter integers
n1 = eval(input("Enter the first integer: "))
n2 = eval(input("Enter the first integer: "))

print("The greatest common divisor for", n1,"and", n2, "is", gcd(n1, n2))
